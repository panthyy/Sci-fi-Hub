{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "dev": "npx wrangler dev --local --persist-to .wrangler/state",
    "db:seed": " shx rm -rf .wrangler/state && yarn db:migrate && ts-node fixtures/seeding/seed.ts",
    "db:generate": "drizzle-kit generate:sqlite",
    "db:up": "drizzle-kit up:sqlite",
    "db:migrate": "wrangler d1 migrations apply scify-db --local",
    "db:migrate:prod": "wrangler d1 migrations scify-db  apply"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "drizzle-orm": "^0.23.13",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "better-sqlite3": "^8.0.1",
    "drizzle-kit": "^0.17.4",
    "shx": "^0.3.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4",
    "vitest": "^0.30.1",
    "wrangler": "^2.15.1"
  }
}
