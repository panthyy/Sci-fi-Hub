{
  "name": "rspack-react-cloudflare",
  "version": "1.0.0",
  "description": "A Opinionated React Starter Template for Cloudflare Workers, using rspack, trpc, twin.macro, react-router and react-query",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && yarn dev\" \"cd client && yarn dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "concurrently": "^8.0.1",
    "vitest": "^0.29.8",
    "wrangler": "^2.14.0"
  },
  "dependencies": {
    "@trpc/client": "^10.18.0",
    "@trpc/server": "^10.18.0"
  }
}
